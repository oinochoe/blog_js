async function main() {
    // 버튼에 이벤트 연결
    // 토큰 체크
    // 토큰으로 서버에서 나의 정보 받아오기
    // 나의 책을 서버에서 받아오기
    // 받아온 책을 그리기
}

// dom 로드 되면 main 실행
document.addEventListener('DOMContentLoaded', main);
